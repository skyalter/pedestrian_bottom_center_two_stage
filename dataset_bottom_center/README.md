# Generated Bottom-Center Dataset

This folder is the output target for `dataset_bbox_to_center/yolo_bbox_to_bottom_center.py`.

It stores a new crop-based YOLO keypoint dataset used by `stage2_bottom_box_center/`.

The source bbox+point dataset in `../dataset_pedestrian_bbox/` remains unchanged.
